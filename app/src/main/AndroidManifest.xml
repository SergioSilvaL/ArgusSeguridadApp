<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.legible.seguridadargusapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Permission for checking if there is an Internet Connection -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".View.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".View.SignInActivity"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <activity
            android:name=".View.GuardiaListaActivity"
            android:parentActivityName=".View.MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".View.GuardiaSignatureActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".View.ConsignasActivity"
            android:label="@string/title_activity_consignas"
            android:parentActivityName=".View.GuardiaListaActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.legible.seguridadargusapp.View.GuardiaListaActivity" />
        </activity>
    </application>

</manifest>